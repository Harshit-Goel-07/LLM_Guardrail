# The dashboard uses Vite's dev proxy (see vite.config.ts) to reach the backend
# at http://localhost:8000, so no env var is required for local development.
#
# If you deploy the built frontend separately and cannot proxy, you can adapt
# src/lib/api.ts to prefix requests with an absolute API base URL.
